# TiB AI Coach — Netlify (Next.js Runtime v5)
# Coach uses split APIs: /api/rag/retrieve then /api/chat (each fits function budget).
# Pro plan: ask Netlify support to raise site function timeout to 26s (not set in this file).

[build]
  command = "npm run build"

[[plugins]]
  package = "@netlify/plugin-nextjs"
